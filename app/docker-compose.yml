services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-3847}:3001"
    volumes:
      - sarif_data:/app/data
    env_file: .env
    environment:
      - NODE_ENV=production

volumes:
  sarif_data:
